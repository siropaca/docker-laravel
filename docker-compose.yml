version: "3.8"

services:
  laravel:
    build: ./php
    volumes:
      - ./laravel:/work

  nginx:
    image: nginx:1.18-alpine
    ports:
      - 10080:80
    volumes:
      - ./laravel:/work
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
    working_dir: /work
